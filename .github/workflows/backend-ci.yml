name: Backend CI

on:
  push:
    branches: [main]
    paths:
      - 'fitforge-backend/**'
  pull_request:
    branches: [main]
    paths:
      - 'fitforge-backend/**'

jobs:
  lint-and-test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: fitforge-backend

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run tests
        run: pytest -q